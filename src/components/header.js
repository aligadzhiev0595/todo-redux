import React from 'react';

const Header = () => {
    return (
        <header className="header">
            <h1 className="title">Todo v.2</h1>
        </header>
    );
};

export default Header;